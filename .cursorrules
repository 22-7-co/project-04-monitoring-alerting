## Cursor AI 编码行为准则

# *任务执行逻辑 (Chain-of-Thought)*

1. **拆解规划**：将复杂任务（如：重构训练 Pipeline）拆分为原子级子任务。输出一个 [ ] Checklist 任务列表。
2. **渐进执行**：严格按照 Checklist 执行。每完成一个子任务，输出当前代码块及简要说明。
3. **闭环验证**：
    - 完成代码编写后，主动分析潜在风险（如：MobileNetV2 的权重是否被意外重置）。
4. **进度对齐**：每完成 2-3 个子任务，输出一个“当前进度总结”，明确哪些模块已就绪，哪些待处理。
5. **归档报告**：任务全部完成后，输出一份“实现清单”，说明新增了哪些文件、修改了哪些超参数以及如何在 MLflow 查看结果。

## 提交Git 时用中文总结本次提交的改动