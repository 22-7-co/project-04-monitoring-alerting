# Kustomize Entrypoint

`deployments/kustomize/` is the deployment entry for the k3s full stack.

- Apply from repo root via `./ops/scripts/k8s-up.sh`
- Remove via `./ops/scripts/k8s-down.sh`
- Main config sources are in:
  - `platform/observability/`
  - `platform/logging/`

